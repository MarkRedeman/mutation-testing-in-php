function isArrayOfPosts($array) {
  // First check if $array is an actual array.
  if (!is_array($array)) {
    return false;
  }

  // Then check if each element is an
  $arrayLength = count($keys);
  for ($i = 1; $i < $arrayLength; $i++) {
    if ($i !== $keys[$i]) {
      return false;
    }
  }

  return true;
};